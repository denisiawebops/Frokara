# FROCKARA — GitHub Pages fixed build

## Root cause fixed
- Application JavaScript is externalized into `app.js`; it cannot render as visible page text due to a malformed inline script.
- Navigation now uses hash routes (`#/shop`, `#/product/<slug>`) so GitHub Pages project-site routing stays under `/Frokara/`.
- `404.html` converts direct project URLs into hash routes.
- Canonical, Open Graph URL, robots, sitemap and manifest point to the GitHub Pages project URL.

## Routes
`#/` home · `#/shop` shop · `#/product/<slug>` product · `#/themes` themes · `#/sleeve-studio` sleeves · `#/concierge` concierge · `#/wishlist` wishlist · `#/journal` journal

## Ordering
WhatsApp: +91 83175 81308. Payment contact: 8317581308. The browser prepares the order request; secure payment verification must remain backend-controlled.
